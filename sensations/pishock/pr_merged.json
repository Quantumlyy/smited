{
  "name": "pr_merged",
  "backend_kind": "pishock",
  "display_name": "PR Merged",
  "description": "Soft vibrate followed by a confirmation beep — your PR landed.",
  "tags": ["dev", "github", "pr", "success"],
  "default_zone_ids": ["shock"],
  "default_intensity": 100,
  "estimated_duration": "0.4s",
  "definition": {
    "microsensations": [
      {
        "parameters": {
          "op":        { "enum_value": "Vibrate" },
          "duration":  { "duration":   "0.2s" },
          "intensity": { "number":     40 }
        }
      },
      {
        "parameters": {
          "op":           { "enum_value": "Beep" },
          "duration":     { "duration":   "0.1s" },
          "intensity":    { "number":     50 },
          "delay_before": { "duration":   "0.1s" }
        }
      }
    ]
  }
}
