@page "/"
@*  smited admin — single-page smoke-test surface. Components added in
    subsequent commits (A2 wires Header / BackendCard / SensationTester /
    PanicButton; A3 wires RecentTriggers).  *@

<h1>smited admin</h1>
<p>v1 placeholder. Components land in subsequent commits.</p>
